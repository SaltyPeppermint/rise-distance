# Analysis notebooks

- `success.ipynb` compares success and peak RSS for selected
  `data/guided_search/run.*` directories.

Shared statistics and plotting code lives in `helpers.py` and `plots.py`.

